name: C++ Memory and FD Leak Checks

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-24.04
    timeout-minutes: 8
    permissions:
      contents: read
      pull-requests: write
      issues: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Valgrind and dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends valgrind gdb

      - name: Create test directory
        run: mkdir -p logs

      - name: Build project
        id: build
        continue-on-error: true
        run: |
          make CFLAGS="-Wall -Wextra -Werror -MMD -g" 2>&1 | tee build_log.txt
          exit ${PIPESTATUS[0]}

      - name: Check build status
        if: steps.build.outcome == 'failure'
        id: build_check
        run: |
          echo "::error::Build failed! Cannot proceed with memory and FD checks."
          echo "error_details<<EOF" >> $GITHUB_OUTPUT
          grep -A 5 "error:" build_log.txt | head -10 >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
          echo "### ❌ Build Failed" >> $GITHUB_STEP_SUMMARY
          echo "Fix compilation errors before running memory checks." >> $GITHUB_STEP_SUMMARY
          echo "```" >> $GITHUB_STEP_SUMMARY
          grep -A 5 "error:" build_log.txt | head -10 >> $GITHUB_STEP_SUMMARY
          echo "```" >> $GITHUB_STEP_SUMMARY
          exit 1

      - name: Run Valgrind checks
        id: valgrind-test
        if: steps.build.outcome != 'failure'
        run: |
          # Write valgrind script
          cat > run_valgrind.sh << 'EOF'
          #!/bin/bash
          exec valgrind --leak-check=full \
                        --show-leak-kinds=all \
                        --track-fds=yes \
                        --child-silent-after-fork=yes \
                        --error-exitcode=1 \
                        --log-file=logs/valgrind.log \
                        ./ircserv 8080 pass
          EOF
          
          chmod +x run_valgrind.sh
          
          # Run the server in background
          ./run_valgrind.sh &
          SERVER_PID=$!
          
          # Wait for server to initialize (5 seconds max)
          for i in {1..5}; do
            if nc -z localhost 8080; then
              break
            fi
            sleep 1
          done
          
          # Check if server is still running
          if ! kill -0 $SERVER_PID 2>/dev/null; then
            echo "::error::Server failed to start properly"
            # Create empty log file so artifact upload doesn't fail
            touch logs/valgrind.log
            exit 1
          fi
          
          # Send shutdown signal
          kill -INT $SERVER_PID
          
          # Wait for graceful shutdown (10 seconds max)
          timeout 10 bash -c "while kill -0 $SERVER_PID 2>/dev/null; do sleep 0.1; done" || \
            (echo "::error::Server did not exit gracefully" && kill -TERM $SERVER_PID && exit 1)
          
          # Process valgrind results
          if [ ! -f logs/valgrind.log ]; then
            echo "::error::Valgrind log file not found"
            # Create empty log file so artifact upload doesn't fail
            touch logs/valgrind.log
            exit 1
          fi
          
          # Check for FD leaks
          if grep -q "FILE DESCRIPTORS: " logs/valgrind.log; then
            fd_leak_count=$(awk '
              /Open file descriptor/ {
                fd_line = $0;
                getline;
                if ($0 !~ /inherited from parent/ && $0 !~ /valgrind\.log/)
                  leaks++;
              }
              END { print leaks+0 }
            ' logs/valgrind.log)
            
            if [ "$fd_leak_count" -gt 0 ]; then
              echo "::error::Found $fd_leak_count file descriptor leaks!"
              echo "fd_leaks=true" >> $GITHUB_OUTPUT
              echo "fd_leak_count=$fd_leak_count" >> $GITHUB_OUTPUT
              echo "fd_leak_details<<EOF" >> $GITHUB_OUTPUT
              awk '
                /Open file descriptor/ {
                  fd_entry = $0;
                  getline;
                  if ($0 !~ /inherited from parent/ && $0 !~ /valgrind\.log/)
                    print fd_entry "\n" $0;
                }
              ' logs/valgrind.log >> $GITHUB_OUTPUT
              echo "EOF" >> $GITHUB_OUTPUT
              exit 1
            fi
          fi

          # Check for memory leaks
          if grep -q "definitely lost:" logs/valgrind.log; then
            memory_leaks=$(grep "definitely lost:" logs/valgrind.log | grep -v "0 bytes in 0 blocks")
            if [ -n "$memory_leaks" ]; then
              echo "::error::Found memory leaks!"
              echo "memory_leaks=true" >> $GITHUB_OUTPUT
              echo "leak_summary<<EOF" >> $GITHUB_OUTPUT
              grep -A 30 "LEAK SUMMARY:" logs/valgrind.log >> $GITHUB_OUTPUT
              echo "EOF" >> $GITHUB_OUTPUT
              exit 1
            fi
          fi
          
          echo "valgrind_passed=true" >> $GITHUB_OUTPUT

      - name: Upload Valgrind log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: valgrind-log
          path: logs/valgrind.log
          if-no-files-found: warn

      - name: Process results
        if: github.event_name == 'pull_request' && always()
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Install GitHub CLI if needed
          if ! command -v gh &> /dev/null; then
            sudo apt-get install -y gh
          fi
          
          # Create summary based on build status
          if [[ "${{ steps.build.outcome }}" == "failure" ]]; then            
            gh pr comment ${{ github.event.pull_request.number }} \
              --body "### ❌ Build Failed

            \`\`\`
            ${{ steps.build_check.outputs.error_details }}
            \`\`\`

            Fix compilation errors before running memory checks.

            📊 [See Build Log](${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID})"
                        gh pr react ${{ github.event.pull_request.number }} --reaction "confused"
                        exit 0
          fi
          
          # Create summary for valgrind results
          if [[ "${{ steps.valgrind-test.outputs.valgrind_passed }}" == "true" ]]; then
            echo "### ✅ Memory and FD Checks Passed" >> $GITHUB_STEP_SUMMARY
            echo "🎉 No memory leaks or file descriptor leaks detected!" >> $GITHUB_STEP_SUMMARY
            
            gh pr comment ${{ github.event.pull_request.number }} \
              --body "### ✅ Memory and FD Checks Passed
                      
            🎉 All checks passed successfully!

            📊 [Full Report](${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID})"
            gh pr react ${{ github.event.pull_request.number }} --reaction "+1"
          else
            FAILURE_MESSAGE="### ❌ Memory and FD Checks Failed\n\n"
            
            if [[ "${{ steps.valgrind-test.outputs.fd_leaks }}" == "true" ]]; then
              FAILURE_MESSAGE+="⚠️ Found ${{ steps.valgrind-test.outputs.fd_leak_count }} file descriptor leaks:\n\n"
              FAILURE_MESSAGE+="\`\`\`\n${{ steps.valgrind-test.outputs.fd_leak_details }}\n\`\`\`\n\n"
            fi
            
            if [[ "${{ steps.valgrind-test.outputs.memory_leaks }}" == "true" ]]; then
              FAILURE_MESSAGE+="⚠️ Found memory leaks:\n\n"
              FAILURE_MESSAGE+="\`\`\`\n${{ steps.valgrind-test.outputs.leak_summary }}\n\`\`\`\n\n"
            fi
            
            FAILURE_MESSAGE+="📊 [Full Report](${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID})"
            
            echo -e "$FAILURE_MESSAGE" >> $GITHUB_STEP_SUMMARY
            echo -e "$FAILURE_MESSAGE" | gh pr comment ${{ github.event.pull_request.number }} --body-file -
            gh pr react ${{ github.event.pull_request.number }} --reaction "eyes"
          fi